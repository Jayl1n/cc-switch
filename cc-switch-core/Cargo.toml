[package]
name = "cc-switch-core"
version = "0.1.0"
description = "Core library for CC Switch — embeddable proxy router, provider management, and AI coding tool configuration"
authors = ["CC Switch Contributors"]
license = "MIT"
edition = "2021"
rust-version = "1.85.0"

[lib]
name = "cc_switch_core"
doctest = false

[features]
default = []

[dependencies]
# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = { version = "1.0", features = ["preserve_order"] }
serde_yaml = "0.9"

# Async runtime
tokio = { version = "1", features = ["macros", "rt-multi-thread", "time", "sync"] }
futures = "0.3"
async-stream = "0.3"

# HTTP / Web framework
axum = "0.7"
tower = "0.4"
tower-http = { version = "0.5", features = ["cors"] }
hyper = { version = "1.0", features = ["full"] }
hyper-util = { version = "0.1", features = ["tokio", "http1", "client-legacy"] }
hyper-rustls = { version = "0.27", features = ["http1", "tls12", "ring", "webpki-tokio"] }
http = "1"
http-body = "1"
http-body-util = "0.1"
httparse = "1"

# TLS
tokio-rustls = "0.26"
rustls = { version = "0.23", default-features = false, features = ["ring"] }
webpki-roots = "0.26"
rustls-native-certs = "0.8"

# Database
rusqlite = { version = "0.31", features = ["bundled", "backup", "hooks"] }

# HTTP client
reqwest = { version = "0.12", features = ["rustls-tls", "json", "stream", "socks"] }

# Data types
indexmap = { version = "2", features = ["serde"] }
rust_decimal = "1.33"
uuid = { version = "1.11", features = ["v4"] }
chrono = { version = "0.4", features = ["serde"] }
bytes = "1.5"

# Parsing
toml = "0.8"
toml_edit = "0.22"
regex = "1.10"
json5 = "0.4"
json-five = "0.3.1"

# Compression (zstd decompression for upstream error responses)
zstd = "0.13"

# Crypto
sha2 = "0.10"
hmac = "0.12"
base64 = "0.22"

# JS engine (for Codex transform)
rquickjs = { version = "0.8", features = ["array-buffer", "classes"] }

# Utilities
log = "0.4"
thiserror = "2.0"
anyhow = "1.0"
once_cell = "1.21.3"
dirs = "5.0"
url = "2.5"
tempfile = "3"
flate2 = "1"
brotli = "7"
zip = "2.2"

[dev-dependencies]
serial_test = "3"
tempfile = "3"

# Windows-only: registry access for env variable management
[target.'cfg(target_os = "windows")'.dependencies]
winreg = "0.52"
windows-sys = { version = "0.61", features = ["Win32_Globalization", "Win32_UI_Shell"] }
